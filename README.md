# 💻 Прогнозирование цен ноутбуков (pipeline)

Этот проект представляет собой веб-сервис, обучающий и использующий модель машинного обучения для прогнозирования цен на ноутбуки. Реализован на FastAPI и XGBoost с возможностью загрузки CSV-файла для получения предсказаний.

---

## 🚀 Запуск приложения

Активируйте свою Conda-среду (если используете):

```bash
conda activate <название_среды>
```

Запустите FastAPI-приложение с автообновлением:

```bash
uvicorn app:app --reload
```

---

## 🌐 Документация (Swagger UI)

После запуска приложение будет доступно по адресу:

- Swagger UI: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)
- OpenAPI спецификация: [http://127.0.0.1:8000/openapi.json](http://127.0.0.1:8000/openapi.json)

---

## 📦 Установка зависимостей

Установите зависимости с помощью pip:

```bash
pip install fastapi uvicorn pandas scikit-learn xgboost joblib
```

Или через Conda:

```bash
conda install -c conda-forge fastapi uvicorn pandas scikit-learn xgboost joblib
```

---

## 📁 Входные данные

Файл с данными `Laptop_price.csv` должен находиться в корне проекта и содержать целевую колонку `Price`.

---

## 📤 Использование API

### POST `/predict`

Загрузите CSV-файл с характеристиками ноутбуков. В ответе будет список предсказанных цен.

Пример запроса через `curl`:

```bash
curl -X POST "http://127.0.0.1:8000/predict" \
  -H "accept: application/json" \
  -H "Content-Type: multipart/form-data" \
  -F "file=@your_file.csv"
```

---

## ✅ Поведение при запуске

- Если модель уже обучена (`laptop_price_model.pkl` существует) — она загружается.
- Если модель отсутствует или повреждена — выполняется автоматическое обучение и сохранение.

---

## 📂 Структура проекта

```
├── app.py                 # Основной сервер FastAPI
├── Laptop_price.csv       # CSV-файл для обучения
├── laptop_price_model.pkl # Сохранённая модель (создаётся автоматически)
└── README.md              # Описание проекта
```

---
